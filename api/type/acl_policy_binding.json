{
  "openapi": "3.0.3",
  "info": {
    "title": "ACL Policy Binding Type",
    "description": "ACL policy binding schema.",
    "version": "1.0.0"
  },
  "paths": {},
  "components": {
    "schemas": {
      "ACLPolicyBinding": {
        "type": "object",
        "required": [
          "id",
          "display_order",
          "policy",
          "created_at",
          "updated_at"
        ],
        "properties": {
          "id": {
            "type": "string"
          },
          "display_order": {
            "type": "number",
            "format": "double"
          },
          "policy": {
            "$ref": "./acl_policy.json#/components/schemas/ACLPolicy"
          },
          "created_at": {
            "type": "string",
            "format": "date-time"
          },
          "updated_at": {
            "type": "string",
            "format": "date-time"
          }
        }
      }
    }
  }
}
