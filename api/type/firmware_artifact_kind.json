{
  "openapi": "3.0.3",
  "info": {
    "title": "Firmware Artifact Kind Type",
    "description": "Firmware artifact kind schema.",
    "version": "1.0.0"
  },
  "paths": {},
  "components": {
    "schemas": {
      "FirmwareArtifactKind": {
        "type": "string",
        "description": "Kind of payload carried by a firmware artifact.",
        "enum": [
          "app",
          "data"
        ],
        "example": "app"
      }
    }
  }
}
